x := i1;
y := i2;
r := 0;
if not y do
	o1 := 0;
else do
	x1 := x;
	
	for i := 1 to x do
		
		x2 := greater(x1, y);
		x3 := equals(x1, y);
		x2 := or(x2, x3);
		for j := 1 to x2 do
			x1 := sub(x1, y); 
			r := r + 1;
		end;
	end;
	o1 := r;
end;

