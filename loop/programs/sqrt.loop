x := i1;
y := 1;
y := y + 1;
x1 := div(x, y);
z := 1;
r := 0;
for w := 1 to x1 do
	k1 := w + 1;
	k1 := mult(k1, k1);

-- if w^2 + 1 > x => w is the result but only the first time
	b := greater(k1, x);
	b := and(b, z);
	for l := 1 to b do
		r := w;
-- flip z to 0 because it acts as a boolean for the first
-- time we exceed x
		z := 0;
	done
done
o1 := r;